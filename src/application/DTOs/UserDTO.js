export default class UserDTO{
    constructor(idUser, names, email, password){
        this.idUser = idUser;
        this.names = names;
        this.email = email;
        this.password = password;
    }
}