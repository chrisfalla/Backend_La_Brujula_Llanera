export class Tag {
    constructor({ idTag, name, createdAt, updatedAt }) {
      this.idTag = idTag;
      this.name = name;
      this.createdAt = createdAt;
      this.updatedAt = updatedAt;
    }
  }