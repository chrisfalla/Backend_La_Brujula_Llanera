export class Category {
    constructor({ idCategory, name, isActive, createdAt, updatedAt }) {
      this.idCategory = idCategory;
      this.name = name;
      this.isActive = isActive;
      this.createdAt = createdAt;
      this.updatedAt = updatedAt;
    }
  }
  